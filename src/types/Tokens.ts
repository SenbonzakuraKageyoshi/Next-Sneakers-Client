interface TokenNames {
    accessToken: "NextSneakersAccessToken";
    refreshToken: "NextSneakersRefreshToken";
};

interface Tokens{
    accessToken: string;
    refreshToken: string;
}

export type {
    Tokens,
    TokenNames
}